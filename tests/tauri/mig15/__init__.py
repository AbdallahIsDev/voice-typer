"""MIG-1.5 Phase 0-W gate-check tests.

This sub-package holds the 9 gate-check test files that validate the
*structure* of the Windows host validation artifacts (build scripts,
docs, configs) from the Linux sandbox. The actual `**VALIDATE ON
WINDOWS HOST**` commands documented in each test's module docstring
MUST be run on a real Windows host — these tests only verify the
artifacts they reference exist and are well-formed.
"""
