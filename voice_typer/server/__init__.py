"""Voice Typer server package.

Contains the Python backend: app orchestrator, ASR engines, recording,
IPC server, tray, etc. The entry point is `voice_typer.server.app:main`
(declared in pyproject.toml as the `voice-typer` console script).
"""
