// BUILD-N11: vitest setup file. Runs before each test file.
// Import @testing-library/jest-dom here once we add DOM matchers.
// For now this is a placeholder so vitest's `setupFiles` resolves.
export {};
