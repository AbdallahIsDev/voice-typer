"""Package marker for ``scripts.release`` — the auto-update publisher lives here.

This package hosts the CI-side release-publishing scripts (distinct from
``voice_typer/server/service/offline_pack.py`` which is the runtime-side pack
downloader owned by Sub-agent 7).
"""
