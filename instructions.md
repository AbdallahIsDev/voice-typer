Only the completed tasks are moved to the DONE.md file. But this file, which is instructions.md, should always stay, whether it's empty or not. Don't delete it ever.

## tasks
Now I want you to commit and push changes to GitHub main branch but not in one commit. I want you to separate the commits related to the changes itself because this all the current changes is related to many different problems we're already fixing, so we can't just push them all together in one commit. All together in one commit we need to separate them into different commits then push them to github I want you to do this right now.


## tasks
Right now when I click when I use a custom seam and then I click restart the electron application completely changed its uh seam to the default with while applying some loading state since the restarted process since the back end restarted, so it's applying some loading state with some description until the back end reconnects and the custom seam re-opties. Seam reapplied. So the problem is the custom seam or the seam reset should not be changed at all whenever I click restart. Right now it changes to the default and then change it back to the custom one after the back end request. Custom one after the back end reconnects after the restart finishes completely you know so we need to fix this the seam should be always applied no no doesn't change at all unless the user changes it manually.


## tasks
In the settings page in the tabs the active I the active item should have the background same as any input field of the settings page for example the app language it has a drop down so this drop down the select background I want the active element to have the same background as the select active element to have the same background as the select background and it should be synced with the seam so whenever the user changes the seam it should also reflect on the active tab the background active tab should be also changed slightly related to the new seam.


## tasks



## tasks



## tasks



## tasks


